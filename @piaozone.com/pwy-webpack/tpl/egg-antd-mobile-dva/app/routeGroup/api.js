// const { API_PRE_PATH } = require('../constants');

module.exports = (app) => {
    // const { router, controller} = app;
    // const topErrCatch = app.middleware.apiOnError();
    // router.post(API_PRE_PATH + '/', topErrCatch, controller.api.index);
};